export const addFunction = (x: number, y: number) => x + y;
export const substractFunction = (x: number, y: number) => x - y;
export const multiplyFunction = (x: number, y: number) => x * y;
export const divideFunction = (x: number, y: number) => x / y;